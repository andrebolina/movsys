import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  root: {},

  media: {
    height: 350,
  },

  title: {
    height: 65,
  },

  rating: {
    textAlign: 'right',
  },

  icon: {
    width: 18,
    marginRight: 5,
  },
});

export default useStyles;
